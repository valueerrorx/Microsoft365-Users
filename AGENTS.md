# Agent bootstrap (Cursor, Claude Code, Codex, etc.)

## First step (every session)

**Before any other tool or implementation work**, run `Read` on `CLAUDE.md` in this repository root.

Do not rely on rule summaries or partial `@` attachments — read the full file.

All behavioral rules, `memory.md` usage, and Graphify requirements live in `CLAUDE.md`.
